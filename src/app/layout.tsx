import type { Metadata } from "next";
import { Inter, Space_Grotesk } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-body",
  subsets: ["latin"],
  display: "swap",
});

const spaceGrotesk = Space_Grotesk({
  variable: "--font-display",
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "NOVA — The Future Has No Weight",
  description:
    "NOVA is a next-generation spatial computing platform that redefines how you interact with digital space. Experience weightless computing.",
  keywords: ["spatial computing", "NOVA", "future", "technology", "3D", "immersive"],
  openGraph: {
    title: "NOVA — The Future Has No Weight",
    description: "Experience the next generation of spatial computing.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body
        className={`${inter.variable} ${spaceGrotesk.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
